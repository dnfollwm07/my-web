# UDP (User Datagram Protocol)

## UDP 特性
- UDP是一種**無連接協議**，優先考慮速度而非可靠性。
- 不保證數據包順序或錯誤檢查，適合視頻流等應用。

## TCP vs UDP 比較
1. TCP是**面向連接**且**可靠**的；UDP是**無連接**且不保證傳送。
2. TCP適合**數據完整性**需求（如文件傳輸）；UDP適合**低延遲**應用（如直播）。
3. TCP有**更多開銷**且**較慢**；UDP更快但**錯誤檢查較少**。 